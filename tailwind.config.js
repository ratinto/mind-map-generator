// tailwind.config.js
import { defineConfig } from 'tailwindcss'

export default defineConfig({
  darkMode: 'class',
  theme: {
    extend: {
      fontFamily: {
        signika: ['Signika', 'sans-serif'],
      },
    },
  },
  plugins: [],
})
